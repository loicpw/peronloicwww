const base = {
    // TODO: colors
    primary: '#154360',
    secondary: '#fbfcfc',
    error: '#cc0000',
    success: '#00bb00',

    // TODO: fonts
};

const small = {
    ...base,
};

const large = {
    ...base,
};

const theme = {
    small, large
};

export default theme;
